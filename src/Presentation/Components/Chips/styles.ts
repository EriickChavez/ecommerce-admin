import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderWidth: 1,
    flexDirection: 'row',
    padding: 5,
    borderRadius: 20,
    alignItems: 'center',
  },
  text: {
    marginHorizontal: 10,
    justifyContent: 'center',
  },
});
